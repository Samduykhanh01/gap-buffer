#include <stdint.h>
#include <stdio.h>
int main(int argc, char *argv[]) {
  printf("The size of a uint8_t is %lu\n", sizeof(uint8_t));
  printf("The size of a char is %lu\n", sizeof(char));
  return 0;
}
